// Promociones de ejemplo (SEED) — fallback solo si NO hay Supabase configurado.
// Vacío a propósito: las promos reales las carga el propietario desde /admin
// (se guardan en la tabla `promociones` de Supabase). Así nunca se muestran
// promociones de ejemplo en el sitio.

export const PROMOS_SEED = []
