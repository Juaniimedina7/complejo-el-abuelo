# Credenciales de Supabase para el panel de administración de promociones.
# Copiar este archivo a ".env" y completar con los valores del proyecto.
# El sitio funciona SIN estas variables (usa promociones de ejemplo);
# solo se necesitan para activar el panel /admin y guardar promos reales.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
